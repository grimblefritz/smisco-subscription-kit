# Composer
/vendor/
/composer.lock

# PHPUnit
/.phpunit.cache/
/.phpunit.result.cache

# Claude Code (project settings shared; local user settings ignored)
/.claude/settings.local.json
/CLAUDE.md

# Local scratch directory
/working/

# Dev-webhost symlink (targets a remote mount; rsynced separately, never committed)
/www

# OS / editor junk
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/
