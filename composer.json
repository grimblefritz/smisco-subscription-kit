{
    "name": "simnuxco/subscription-kit",
    "description": "Stripe-backed subscription model (Checkout, Portal, webhook receiver, access gate) shared across simnuxco apps.",
    "type": "library",
    "license": "proprietary",
    "require": {
        "php": ">=8.1",
        "stripe/stripe-php": "^17.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.0"
    },
    "autoload": {
        "psr-4": {
            "Simnuxco\\SubscriptionKit\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Simnuxco\\SubscriptionKit\\Tests\\": "tests/"
        },
        "exclude-from-classmap": [
            "tests/run.php"
        ]
    },
    "config": {
        "sort-packages": true,
        "optimize-autoloader": true
    }
}
